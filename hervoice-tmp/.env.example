# hervoice 配置 — 复制为 .env 填写，别把 .env 提交进 git

# 转写：Groq Whisper（免费额度足够个人用）https://console.groq.com
GROQ_API_KEY=

# 情感分析 LLM：任何 OpenAI 兼容接口都行（默认 DeepSeek，便宜）
LLM_API_KEY=
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat

# 转写模型和语言
WHISPER_MODEL=whisper-large-v3
WHISPER_LANG=zh

# 分析出结果后 POST 到这个 URL — 接你自己的 AI 助手/心跳/通知。留空则不回调。
# 收到的 JSON: {ts,text,emotion,confidence,hint,features,audio}
HERVOICE_WEBHOOK=

# 数据目录
HERVOICE_DATA=./data

# 是否留存音频。默认 0 = 阅后即焚（只留文字和情感）。
# 设 1 = 保留 mp3，可通过 /api/voice/audio/<name> 重听。留存是隐私决定，自己权衡。
KEEP_AUDIO=0
